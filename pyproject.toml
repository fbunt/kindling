[project]
name = "natlangq"
version = "0.1.0"
description = "A natural language query tool"
requires-python = ">=3.12"
dependencies = [
    "fastapi",
    "uvicorn[standard]",
    "google-genai",
    "itsdangerous",
    "python-dotenv",
    "python-multipart",
]
